This is a project by emperor42, it creates a web crawler which creates a visual version of all links of a given url. The urls and links identified should be stored in summary detail html element pairs where the details element shows a copy of the content from he link and a button to continue down the path. There should be a breadcrumb path on the top left showing how far we have crawled. 